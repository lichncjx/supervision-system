# 群晖部署说明

## 部署目录建议

```
/volume1/docker/supervision-system/
├── docker-compose-synology.yml
├── .env
├── deploy.sh
├── postgres_data/
└── uploads/
```

## 首次部署步骤

1. 登录群晖，打开终端或 SSH
2. 创建部署目录：
   ```bash
   mkdir -p /volume1/docker/supervision-system
   cd /volume1/docker/supervision-system
   ```
3. 将以下文件复制到部署目录：
   - `docker-compose-synology.yml`
   - `.env.example` → 重命名为 `.env`
   - `deploy.sh`
4. 修改 `.env` 文件，填写真实配置：
   ```env
   POSTGRES_PASSWORD=你的强密码
   JWT_SECRET=你的JWT密钥（足够复杂的随机字符串）
   NEXT_PUBLIC_APP_URL=http://你的群晖IP:18080
   ```
5. 登录 GitHub Container Registry：
   ```bash
   docker login ghcr.io
   ```
   > 使用 GitHub 用户名和 Personal Access Token（需要 read:packages 权限）
6. 给部署脚本添加执行权限：
   ```bash
   chmod +x deploy.sh
   ```
7. 执行部署：
   ```bash
   sh deploy.sh
   ```

## 日常更新步骤

```bash
cd /volume1/docker/supervision-system
sh deploy.sh
```

部署脚本会自动：
1. 拉取最新镜像
2. 启动数据库
3. 执行数据库迁移
4. 启动应用
5. 清理悬空镜像

## 查看日志

```bash
# 查看应用日志
docker logs -f supervision-app

# 查看迁移日志
docker logs supervision-migrate

# 查看数据库日志
docker logs -f supervision-db
```

## 查看容器状态

```bash
docker compose -f docker-compose-synology.yml ps
```

## 停止服务

```bash
docker compose -f docker-compose-synology.yml down
```

## 只重启 app

```bash
docker compose -f docker-compose-synology.yml up -d app
```

## 手动执行数据库迁移

```bash
docker compose -f docker-compose-synology.yml run --rm migrate
```

## 健康检查

应用提供健康检查接口：
- 地址：`http://你的群晖IP:18080/api/health`
- 响应：
  ```json
  {
    "status": "ok",
    "service": "supervision-system",
    "timestamp": "2026-05-03T10:00:00.000Z"
  }
  ```

## 常见问题排查

### GHCR 登录失败

**问题**：`docker login ghcr.io` 认证失败

**解决**：
1. 确认使用 GitHub Personal Access Token 而非密码
2. Token 需包含 `read:packages` 权限
3. 访问：https://github.com/settings/tokens 创建 Token

### 镜像拉取慢

**问题**：从 ghcr.io 拉取镜像速度慢

**解决**：
1. 配置 Docker 镜像加速（需要在群晖 Docker 注册表中配置）
2. 或者选择网络状况较好的时段更新

### 端口 18080 被占用

**问题**：启动时提示端口已被占用

**解决**：
1. 修改 `docker-compose-synology.yml` 中的端口映射：
   ```yaml
   ports:
     - "你的端口:5000"
   ```
2. 同时更新 `.env` 中的 `NEXT_PUBLIC_APP_URL`

### .env 缺失

**问题**：提示 `.env` 文件未找到

**解决**：
1. 复制 `.env.example` 为 `.env`
2. 填写必要的配置项

### POSTGRES_PASSWORD 不一致

**问题**：数据库密码与之前不同导致无法连接

**解决**：
1. 如果是新部署，确认密码一致
2. 如果是旧部署，**不要修改** `POSTGRES_PASSWORD`，保持与首次部署时一致
3. 如果确实需要修改，需要删除 `postgres_data` 并重新初始化（会清空所有数据）

### migrate 执行失败

**问题**：迁移容器执行失败或退出码非 0

**解决**：
1. 查看迁移日志：`docker logs supervision-migrate`
2. 确认数据库已正常启动
3. 检查 `DATABASE_URL` 配置是否正确
4. 手动执行迁移：`docker compose -f docker-compose-synology.yml run --rm migrate`

### app 容器反复重启

**问题**：应用容器不断重启

**解决**：
1. 查看应用日志：`docker logs -f supervision-app`
2. 检查数据库连接是否正常
3. 检查 `.env` 配置是否正确
4. 确认数据库迁移已成功执行

### 页面无法访问

**问题**：浏览器无法打开系统

**解决**：
1. 确认容器正在运行：`docker compose -f docker-compose-synology.yml ps`
2. 检查端口是否正确映射
3. 检查群晖防火墙是否允许 18080 端口
4. 检查 `NEXT_PUBLIC_APP_URL` 是否正确配置

## 注意事项

### ⚠️ 重要提示

1. **不要删除 `postgres_data` 目录**，除非确认要清空数据库
2. **不要删除 `uploads` 目录**，避免丢失上传的附件
3. **不要在群晖本地构建镜像**，所有镜像都从 GHCR 拉取
4. **app 镜像和 migrate 镜像都从 GHCR 拉取**
5. **deploy.sh 会执行数据库迁移**，确保备份重要数据
6. **deploy.sh 不会清空数据库**，只会应用新的迁移

### 数据安全

- 定期备份 `postgres_data` 和 `uploads` 目录
- 部署前先在测试环境验证
- 生产环境更新前先备份数据库

### 环境变量

- `POSTGRES_PASSWORD`：首次设置后不要随意更改
- `JWT_SECRET`：更改会导致所有用户需要重新登录
- `NEXT_PUBLIC_APP_URL`：确保与实际访问地址一致

## 技术架构

### 镜像说明

| 镜像 | 用途 | 大小 |
|------|------|------|
| `ghcr.io/lichncjx/supervision-system:latest` | Next.js 应用 | ~200-300MB |
| `ghcr.io/lichncjx/supervision-system-migrate:latest` | 数据库迁移 | ~100-200MB |

### 服务依赖

```
db (PostgreSQL)
  ↓
migrate (执行迁移)
  ↓
app (Next.js 应用)
```

### 部署流程

1. 启动数据库
2. 执行数据库迁移
3. 启动应用
4. 清理悬空镜像