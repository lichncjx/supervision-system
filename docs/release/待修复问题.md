# 待修复问题

本文只记录当前 target-contract 或阶段边界下仍需后续处理的问题。已在 Phase 8C 收口并通过验证的事项不再列为待修复。

## 当前验证状态（Phase 8D）

```text
target-contract: 120 PASS, 0 FAIL
regression-baseline: 38 PASS, 0 FAIL
tsc: 0 errors
lint: 0 errors, 4 warnings (pre-existing)
prisma validate: OK
```

## Phase 8 已完成，不再列为待修复

1. 责任方模型统一：`departmentId` + `responsibleLeader` + `responsiblePerson` + `cooperators`。
2. 旧字段删除：`responsibleDepartmentIds`、`cooperateDepartmentIds`、`responsiblePersons`、`cooperatePersons`。
3. 页面/表单/Excel/Dashboard/API 全部适配新模型。
4. 完成率只按 `departmentId`，cooperators 不计入。
5. cooperators[].departmentId 参与可见范围。
6. 姓名文本字段不参与权限。
7. Dashboard 轻量列表返回 `cooperators` / `responsibleLeader` / `responsiblePerson`。
8. Excel 导入导出覆盖 todo/priority/main + cooperators[].leader。
9. target-contract 覆盖全部新模型断言。

## 已知后续事项（不在 Phase 8 范围）

1. SUPERVISOR 督办跟踪拆分。
2. `pnpm build` Windows symlink EPERM（环境问题，非代码问题）。
3. Prisma JSONB `cooperators` 路径过滤优化（当前使用 broad query + post-filter）。
4. 如需人员维度统计/通知/权限控制，再评估 `DepartmentMember` / `WorkParticipant`。
12. `tests/regression-baseline.cjs` 已重写为 9 状态轻量回归脚本。

## 当前仍需后续处理

1. **SUPERVISOR 督办跟踪与普通待处理口径拆分**
   - 当前 `SUPERVISOR` 仍有“督办跟踪”特殊展示语义。
   - 后续应拆为独立字段、独立列表或独立页面，不再与普通用户“待处理 = 待审批 + 待办理”混用。

2. **业务人员体系增强**
   - `responsiblePersons`、`cooperatePersons`、`responsibleLeader`、`responsiblePerson` 当前仍作为姓名文本。
   - 本轮不新增 `Member`、`DepartmentMember`、`WorkParticipant`。人员重名、调岗、无法按人员 ID 统计等风险留待后续评估。

3. **发布前综合验收**
   - 第六阶段完成后，仍需按 `docs/release/回归测试清单.md` 和 `docs/release/测试发布检查清单.md` 执行完整发布前验证。
