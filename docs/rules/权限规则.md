# 权限规则

## 零、统一权限模块

第二阶段新增/完善 `src/lib/server-permissions.ts`，作为服务端权限和数据范围的集中入口。第六阶段 PR 6.2 已将 workflow POST 的审批和办理判断同步到该模块。

当前统一 helper 包括：

1. `getResponsibleDepartmentIds(work)`：返回 `work.departmentId ? [work.departmentId] : []`（Phase 8B 统一为单一主责部门）。
2. `getCooperatorDepartmentIds(work)`：从 `work.cooperators` 解析 `departmentId` 并返回配合部门 ID 数组。`getCooperateDepartmentIds` 已标记为 deprecated，指向 `getCooperatorDepartmentIds`。
3. `buildWorkVisibilityWhere(user)`：构造事项可见范围查询条件。
4. `canViewWorkItem(user, work)`：事项查看权限。
5. `canApproveWorkItem(user, work)`：当前审批节点权限。
6. `shouldHandleWorkItem(user, work)`：待办理判断（基于 `canOperateWorkItem` 收窄）。
7. `canOperateWorkItem(user, work)`：业务办理 / workflow 操作权限，包含事项状态判断；全局查看角色不因此获得办理权。
8. `canEditWorkItem(user, work)`：草稿/退回编辑权限，包含 `DRAFT` 状态判断；全局查看角色不因此获得编辑权。
9. `canViewAttachment(user, work)`、`canUploadAttachment(user, work)`、`canDeleteAttachment(user, work, attachment)`：附件权限。

已接入范围：

1. `GET /api/works`
2. `GET /api/works/[id]`
3. `PUT /api/works/[id]`
4. `GET /api/works/[id]/workflow`
5. `GET /api/dashboard/summary`
6. `GET /api/dashboard/completion-rate`
7. `GET /api/excel/completion-rate`
8. `GET /api/excel/export`
9. `POST /api/excel/import/[type]`
10. `src/lib/attachment-permissions.ts` 及上传/下载/删除附件接口
11. `POST /api/works/[id]/workflow`

PR 6.2 后 workflow 相关口径：

1. 审批态为 `PROPOSING`、`ADJUSTING`、`CANCELLING`、`COMPLETING`。
2. `canApproveWorkItem` 优先按 `currentApproverId` 精确匹配当前用户。
3. `currentApproverRole` 作为审批节点辅助，部门级审批还必须匹配主责部门范围。
4. 公司级审批尽量写入精确 `currentApproverId`，避免按 `VICE_PRESIDENT` 角色默认放开。
5. `shouldHandleWorkItem` 支持部门角色办理 `PENDING_DECOMPOSE`；`DRAFT` 仅 `creatorId` 对应创建人可办理，立项审批退回不改变草稿权属；审批退回后的 `IN_PROGRESS` 由 `responsiblePersonUserId` 办理，普通 `IN_PROGRESS` 不默认进入待办理。

## 一、核心原则

1. 查询统计按组织权限。
2. 办理操作按具体业务动作判定：待分解按主责部门部门级角色；草稿及立项审批退回草稿统一按创建人；审批通过进入 `IN_PROGRESS` 后按 `responsiblePersonUserId`。
3. 审批操作按 `currentApproverId` / `currentApproverRole`。
4. 核心内容修改必须走 `ADJUSTING`。
5. `User` 是系统登录账号，责任领导、责任人、主责责任人、配合责任人是业务责任人姓名记录。
6. `DEPT_MANAGER` 是部门事项管理岗，不是事项实际责任人。
7. `DEPT_LEADER` 和 `DEPT_MANAGER` 查看本部门相关事项时，不应按 `creatorId` / `firstSubmitterId` 限制。
8. 公司领导可查看全公司非草稿事项，但不能因此修改或办理其他领导负责事项；草稿仍围绕本人提出、分管、当前审批或明确授权范围。
9. `SUPERVISOR` 和 `ADMIN` 可以按全局口径查看。
10. 全局查看权限不等同于业务办理权限；草稿编辑、提交审批、待分解、完成、调整、取消等业务流转动作必须按对应状态和办理人判断。

## 二、User 与业务责任人边界

`User` 用于登录认证、审批权限判断、办理权限判断、上传和删除附件权限判断、数据可见范围判断、workflow record 审计。

业务责任人用于事项展示、责任留痕、导入导出、历史档案。业务责任人不一定登录系统，也不参与系统审批、办理、数据可见权限判断。

## 三、关键系统字段

| 字段 | 权限语义 |
| --- | --- |
| `creatorId` | 系统创建人 / 系统填报人 / 事项在系统中的经办账号 |
| `firstSubmitterId` | 首次提交人，用于流程审计和展示；不参与 `DRAFT` 办理、编辑或删除权限 |
| `currentApproverId` | 当前审批节点的精确用户 |
| `currentApproverRole` | 当前审批节点的角色 |
| `departmentId` | 唯一主责部门 |
| `cooperators` | 配合方列表 JSON |
| `responsibleLeaderUserId` | 责任领导系统用户，查看/监督，不默认办理 |
| `responsiblePersonUserId` | 责任人系统用户，IN_PROGRESS 后唯一办理人 |

主责部门通过 `getResponsibleDepartmentIds(work)` 统一读取，返回 `[work.departmentId]`。
配合部门通过 `getCooperatorDepartmentIds(work)` 从 `work.cooperators` 解析。

## 四、查看权限

1. `ADMIN`、`SUPERVISOR` 按全局口径查看。
2. `DEPT_LEADER`、`DEPT_MANAGER` 可查看本部门相关事项：
   - `departmentId` 为本部门；
   - `cooperators[].departmentId` 包含本部门。
3. 部门角色查看本部门相关事项时，不按 `creatorId` 或 `firstSubmitterId` 限制。
4. `VICE_PRESIDENT`、`PRESIDENT` 可查看全公司所有非 `DRAFT` 事项，包括重点工作、主要工作和待办事项。
5. 公司领导对 `DRAFT` 事项继续沿用原有关联范围：本人是 `proposedLeaderId`、`approvalLeaderId` 或 `currentApproverId` 时可见；其他领导关联的草稿不扩散。
6. 本次不新增 `responsiblePersonUserId` / `responsibleLeaderUserId` 本人可见条件；责任人继续使用部门角色，沿用部门可见范围。
7. 公司领导扩大的只是查看权限，不产生对其他领导事项的审批、编辑、办理、附件上传删除或状态流转权限。

## 五、办理权限

办理指不需要当前用户审批，但需要当前用户推进事项的操作，例如待分解、提交材料、退回后修改。

1. `PENDING_DECOMPOSE` 是部门动作，仅主责部门部门级角色（`DEPARTMENT_MANAGER`、`DEPARTMENT_LEADER`）可办理。
2. `DRAFT` 草稿统一由 `creatorId` 对应创建人办理；立项审批退回不改变草稿权属，`firstSubmitterId` 不参与 `DRAFT` 权限判断。
3. `PROPOSE` 审批通过进入 `IN_PROGRESS` 后，`responsiblePersonUserId` 成为唯一办理人；调整、取消、完成提交，以及这些审批退回后的继续处理，均沿用该责任人系统用户。
4. 普通 `IN_PROGRESS` 事项不默认计入待办理；只有审批退回后需要当前用户推进时才进入待办理。
5. 同部门其他人员可见但不因同部门获得操作权。
6. `DEPT_MANAGER` / `DEPT_LEADER` 可查看本部门相关事项，但查看权限不等同于办理权限。
7. 责任领导、责任人、主责责任人、配合责任人姓名不产生办理权限。
8. 配合部门是否可办理应按具体动作定义；默认配合部门只参与可见和协同，不自动获得主责部门办理权。
9. `IN_PROGRESS` 后责任人调整必须走 `ADJUSTING` 审批，普通更新接口不得直接修改 `responsiblePersonUserId`。
10. `ADMIN` / `SUPERVISOR` 的全局查看权限不放开业务办理动作；如需管理维护能力，应通过附件管理或后续单独管理函数表达。

## 六、事项删除权限

1. 事项删除只允许当前状态为 `DRAFT` 的事项。
2. 创建人可以永久删除自己的 `DRAFT`，包括已经产生立项审批记录、审批退回后恢复为 `DRAFT` 的事项。
3. `ADMIN` 可以永久删除任意创建人的 `DRAFT`，但不能删除任何非 `DRAFT` 事项。
4. `SUPERVISOR`、同部门其他用户及其他角色不因查看、办理或全局数据范围获得事项删除权。
5. 删除整个 `DRAFT` 时，审批记录、附件记录及其他从属数据随事项级联删除；审批记录不提供单独删除能力。
6. 删除操作日志独立保留，至少记录实际删除人、创建人、原事项 ID、事项标题以及流程记录和附件数量摘要。
7. 删除事项后应清理附件物理文件；数据库删除和操作日志必须在同一事务中完成。

## 七、审批权限

1. 审批权限只按当前审批节点判断。
2. `currentApproverId` 等于当前用户 ID 时可审批。
3. `currentApproverRole` 等于当前用户角色时可审批；部门级审批必须同时匹配部门范围。
4. 公司级审批不应通过”公司领导角色泛化”默认放开，应尽量使用精确 `currentApproverId`。
5. `approvalType` 用于区分当前审批是立项、调整、取消还是完成，不替代审批人判断。
6. 审批通过时如果还有下一节点，只切换当前审批人，状态保持当前审批态。

## 八、编辑与调整权限

1. 草稿和待修改内容可以由有办理权限的用户维护。
2. 事项进入 `IN_PROGRESS` 后，核心内容修改必须走 `ADJUSTING`。
3. 普通编辑接口不得直接修改状态、当前审批人或审批辅助字段。
4. 公司领导之间不能默认互相修改对方负责事项。
5. 业务责任人姓名字段不产生编辑权限。

## 九、附件权限

> 附件权限原则详见 [业务规则](../core/业务规则.md) §一-12。
> 附件权限详细矩阵见 [业务人员与附件权限规则](业务人员与附件权限规则.md) §六。

## 十、导入导出权限

> 导入导出权限原则详见 [业务规则](../core/业务规则.md) §一-9。
> 导入导出详细规则见 [导入导出规则](导入导出规则.md) §六。
# PR 6.3 当前补充

1. 当前系统运行态已经切换为 9 状态，审批态只包括 `PROPOSING`、`ADJUSTING`、`CANCELLING`、`COMPLETING`。
2. `APPROVED` 和 `REJECTED` 不再作为当前权限判断状态。
3. “退回待修改”通过业务状态 + 退回痕迹派生：退回草稿仍由 `creatorId` 对应创建人办理，退回 `IN_PROGRESS` 由 `responsiblePersonUserId` 办理。
4. `shouldHandleWorkItem` 对退回事项不按同部门泛化放开，避免同部门其他主管/领导误获得修改权限。
5. 普通编辑接口不得写 `beforeApprovalStatus`、`approvalType` 或直接修改事项状态；状态流转必须经过 workflow。
