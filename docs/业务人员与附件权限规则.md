# 业务人员与附件权限规则

> 本文档是业务人员字段命名体系、人员体系分类、附件机制和附件权限规则的集中参考。
> 权限、审批流、状态机、统计口径的权威文档仍是 `docs/业务规则.md`。
> 当前代码仍保留若干历史字段；本文件先写目标口径，再说明当前实现兼容字段。

## 一、业务人员字段命名规则

目标架构先区分两类人员信息：

1. `User`：系统登录账号，用于登录、审批、办理、上传、权限判断和流程审计。
2. 业务责任人：姓名文本，用于事项展示、责任留痕、导入导出，不一定登录系统，也不参与审批、办理、数据可见权限判断。

系统用户字段通常遵循 **xxxId / xxxName** 约定；业务责任人字段不按 User 外键理解。

### xxxId（真实关联字段）

- 类型：`Int` 或 `Int?`，FK 指向 `users.id`
- 用途：权限判断、流程路由、审批指派、上传权限、退回处理人判定、待处理统计
- 特征：有值时必须指向有效的用户记录；服务端校验用户存在性、所属部门、角色和启用状态

### xxxName（历史快照字段）

- 类型：`String?`，纯文本
- 用途：页面展示、Excel 导出、历史留痕
- 特征：创建/更新时从 User 记录同步写入，后续即使 User 姓名变更也不回溯更新
- 没有 FK 约束，不参与权限判断

### Phase 7 已清理的旧字段

以下字段已在 Phase 7 中从 schema 和代码中删除：`deptLeaderId`、`deptLeaderName`、`deptManagerId`、`deptManagerName`、`supervisor`。

### 当前字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `responsibleLeader` | `String?` | 责任领导姓名文本，仅展示/导入导出/留痕，不参与权限 |
| `responsiblePerson` | `String?` | 责任人/实际负责人姓名文本，仅展示/导入导出/留痕，不参与权限 |
| `responsiblePersons` | `String[]` | 待办主责责任人姓名数组，仅展示/导入导出/留痕 |
| `cooperatePersons` | `String[]` | 待办配合责任人姓名数组，仅展示/导入导出/留痕 |

**权限判断原则：审批看 `currentApproverId` / `currentApproverRole`，办理看部门管理权限，业务责任人姓名文本不参与权限判断。**

---

## 二、重点工作 / 主要工作人员体系

重点工作（`PRIORITY`）和主要工作（`MAIN`）目标上填写业务责任领导和事项实际责任人，二者都是姓名文本。

### 字段一览

| 字段 | 类型 | 说明 |
|------|------|------|
| `departmentId` | `Int?` | 单一主办部门 ID（FK → departments） |
| `responsibleLeader` | `String?` | 事项业务责任领导姓名文本，不是 User 外键，不是 Member 外键 |
| `responsiblePerson` | `String?` | 事项实际责任人姓名文本，不是 User 外键，不是 Member 外键 |

### 关键规则

1. `responsibleLeader` / `responsiblePerson` 只用于展示、导入导出和历史留痕。
2. 二者不参与审批权限、办理权限、数据可见权限判断。
3. `creatorId` 才是系统创建人 / 系统填报人 / 经办账号。
4. `currentApproverId` / `currentApproverRole` 才是审批权限判断依据。
5. `DEPARTMENT_MANAGER` 是部门事项管理岗，不是事项实际责任人。
6. `supervisor` 不是 `User.role = SUPERVISOR`，也不是目标责任字段。

---

## 三、待办事项人员体系

待办事项（`TODO`）采用**多部门、多责任人体系**。

### 字段一览（Phase 8B 统一）

| 字段 | 类型 | 说明 |
|------|------|------|
| `departmentId` | `Int?` | 唯一主责部门 |
| `responsibleLeader` | `String?` | 主责责任领导姓名文本，仅用于展示、导入导出、历史留痕 |
| `responsiblePerson` | `String?` | 主责责任人姓名文本，仅用于展示、导入导出、历史留痕 |
| `cooperators` | `Json?` | 配合方列表 `[{ departmentId, departmentName?, leader?, person? }]` |

已删除字段：`responsibleDepartmentIds`、`cooperateDepartmentIds`、`responsiblePersons`、`cooperatePersons`（Phase 8B）。

### 关键规则

1. 本轮不新增 `Member` / `DepartmentMember` / `WorkParticipant` 表。
2. 责任领导/责任人/配合方 leader/person 为姓名文本，不做 userId 化。
3. 姓名文本字段不参与审批权限、办理权限、数据可见权限判断。
4. 配合方 `cooperators[].departmentId` 参与可见范围判断，不计入完成率。
5. `getResponsibleDepartmentIds(work)` 返回 `[work.departmentId]`。
6. `getCooperatorDepartmentIds(work)` 从 `cooperators` JSON 解析配合部门 ID。

---

## 四、公司领导字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `proposedLeaderId` | `Int?` | 提出领导 ID（`VICE_PRESIDENT` 或 `PRESIDENT`），FK → users |
| `approvalLeaderId` | `Int?` | 审批领导 ID，负责调整、取消、完成审批 |
| `proposedLeader` | relation | 前端展示字段，由 Prisma relation 从 `proposedLeaderId` 解析 |
| `approvalLeader` | relation | 前端展示字段，由 Prisma relation 从 `approvalLeaderId` 解析 |

### 关键规则

1. `approvalLeaderId` 默认等于 `proposedLeaderId`（创建/导入/表单三处统一）。
2. 特殊情况下二者可以不同（如提出领导变更或指定其他审批人）。
3. `proposedLeader` / `approvalLeader` 如果在前端存在，仅作为展示字段或由 relation 解析，不作为数据库主字段。
4. 后续如有迁移，可考虑 `proposedLeaderName` / `approvalLeaderName` 命名方式。

---

## 五、证明材料 / 附件机制

### 存储模型

- 通用附件和证明材料**统一存储在 `attachments` 表**。
- `attachments.category` 字段区分类型：
  - `"general"` — 普通附件（默认值，兼容历史数据）
  - `"evidence"` — 完成证明材料 / 见证材料

### 上传流程

```
WorkOperationPanel 选择文件
  → POST /api/upload (category=evidence)
  → 文件写入磁盘 + attachments 表记录
  → 返回 attachment 对象
  → 前端收集 evidence attachment 列表
  → 点击"提交完成材料"
  → POST /api/works/[id]/workflow { action: 'evidence', proof }
  → submitEvidence 仅处理 proof 文本和状态流转
```

### 前端展示规则

| 区域 | 显示内容 | 数据来源 |
|------|---------|---------|
| 普通附件面板（WorkAttachmentPanel） | `category != "evidence"` 的附件 | `work.attachments` 过滤 |
| 证明材料区 | `category == "evidence"` 的附件 | `work.attachments` 过滤 |
| 证明材料文字 | `proof` 文本 | `work_items.proof` |

### 与旧系统的区别

| 维度 | 旧方案 | 新方案 |
|------|--------|--------|
| 证明文件存储 | 前端 React state（base64），页面刷新丢失 | `attachments` 表持久化 |
| 证明文件传输 | `submitComplete` 丢弃 `_proofFiles` 参数 | 通过 `/api/upload` 上传后 workflow 引用 |
| 附件分类 | 无分类 | `category` 字段区分 `general` / `evidence` |
| 详情页证明展示 | 死代码，永远不执行 | 从 `work.attachments` 按 category 筛选展示 |

### 不变的部分

- `proof` 文本字段仍只保存文字说明。
- `submitEvidence`（`src/lib/workflow.ts`）不改，继续只处理 proof 文本和状态流转。
- workflow `action=evidence` 路由不改，不从 body 解构文件数据。

---

## 六、附件权限规则（当前实现差异）

附件权限当前统一由 `src/lib/attachment-permissions.ts` 模块集中管理。三个 API 端点（`/api/upload`、`/api/attachments/[id]`、`/api/attachments/[id]/download`）统一调用此模块的三个导出函数。

以下规则描述当前实现；目标上附件权限应与统一事项查看权限、部门办理权限和当前动作权限对齐，不应继续把业务责任人姓名字段作为权限依据。

### 6.1 查看 / 下载权限 (`canViewAttachment`)

```
ADMIN / SUPERVISOR                     → 全权限
事项创建人 (creatorId)                  → 可查看下载
同部门用户 (departmentId 匹配)          → 可查看下载
VICE_PRESIDENT / PRESIDENT             → 公司级可见（isCompanyLevel）
```

### 6.2 上传权限 (`canUploadAttachment`)

```
ADMIN / SUPERVISOR                                          → 可上传
同部门 DEPARTMENT_MANAGER / DEPARTMENT_LEADER (非 terminated) → 可上传
同部门 DEPARTMENT_MANAGER / DEPARTMENT_LEADER (rejected)     → 不可上传
VICE_PRESIDENT / PRESIDENT                                  → 不可上传
配合部门用户                                                  → 不因配合身份获得上传权限
终止态 (completed / cancelled)                               → 不可上传
```

### 6.3 删除权限 (`canDeleteAttachment`)

删除权限已收紧（Phase 3C），当前规则：

```
ADMIN              → 可删除所有附件
SUPERVISOR         → 可删除所有附件
上传者本人           → 可删除自己上传的附件
同部门 DEPARTMENT_MANAGER / DEPARTMENT_LEADER → 不再默认删除他人附件
配合部门用户         → 不能删除他人附件
```

**历史变更**：Phase 3C 之前，同部门 `DEPARTMENT_MANAGER` / `DEPARTMENT_LEADER` 可以删除部门下所有附件。该宽权限已被移除，统一收紧为上传者本人才能删除。

### 6.4 权限模块接口

```typescript
// src/lib/attachment-permissions.ts

interface AttPermUser {
  id: number;
  role: string;
  departmentId: number;
}

interface AttPermWorkItem {
  departmentId: number | null;
  status: string;
  creatorId: number;
  type: string; // 'PRIORITY' | 'MAIN' | 'TODO'
}

interface AttPermAttachment {
  userId: number;
}

canViewAttachment(user, workItem): boolean;
canUploadAttachment(user, workItem): boolean;
canDeleteAttachment(user, workItem, attachment): boolean;
```

---

## 七、数据库字段变更记录

### Phase 2 — WorkItem 新增字段（Phase 7 已删除）

以下字段为 Phase 2 新增，Phase 7 已从 schema 中删除：

```prisma
// 已删除: deptLeaderId, deptManagerId, deptLeaderName, deptManagerName, supervisor
```

### Phase 8B — 责任方模型统一

```prisma
departmentId       Int?
responsibleLeader  String?  @db.VarChar(50)  // 责任领导姓名文本
responsiblePerson  String?  @db.VarChar(50)  // 责任人姓名文本
cooperators        Json?                     // 配合方 [{ departmentId, departmentName?, leader?, person? }]
```

`responsibleDepartmentIds` / `cooperateDepartmentIds` / `responsiblePersons` / `cooperatePersons` 已删除。

- 有 Prisma relation（`deptLeader` / `deptManager`），`onDelete: SetNull`
- 历史数据需要执行回填 SQL

### Phase 3A — Attachment 新增字段

```prisma
category  String  @default("general")  @db.VarChar(30)
```

- 默认 `"general"`，存量附件自动兼容
- 无需回填

---

## 八、部署 / migration 注意事项

1. Vercel Preview 环境**不会自动执行数据库 migration**，需手动执行 `pnpm prisma:deploy` + `pnpm prisma:generate`。
2. Supabase / 本地 Docker / 群晖内网数据库都需要执行 `prisma migrate deploy`。
3. 执行 migration 后需要 `prisma generate`，确保 Prisma Client 类型一致。
4. 涉及 Phase 2 migration 时，除 DDL 外还需执行回填 SQL。
5. 当前实现兼容字段未删除；`responsibleLeader` 目标语义调整为事项业务责任领导姓名文本，`supervisor` 仅作为历史兼容字段，不作为目标责任字段。
6. `attachments.category` 默认 `general`，可兼容存量附件，无需额外操作。
7. Phase 1 无数据库结构变化。

---

## 九、相关文件索引

| 文件 | 内容 |
|------|------|
| `prisma/schema.prisma` | WorkItem、Attachment 模型完整定义 |
| `src/lib/attachment-permissions.ts` | 附件权限统一判断模块 |
| `src/lib/server-permissions.ts` | 服务端事项权限判断（含 cooperateDepartmentIds） |
| `src/lib/workflow.ts` | 状态流转（含 submitEvidence） |
| `src/lib/work-store.ts` | 前端 Work/Attachment 接口、展示、筛选逻辑 |
| `src/app/api/upload/route.ts` | 文件上传接口（含 category 参数） |
| `src/components/work/work-attachment-panel.tsx` | 普通附件面板 |
| `src/components/work/work-operation-panel.tsx` | 证明材料操作面板 |
| `docs/业务规则.md` | 权限、审批流、状态机、统计口径权威文档 |
| `docs/项目说明.md` | 项目背景、目录结构、API 概览 |
| `docs/部署说明-群晖.md` | 群晖部署和迁移说明 |

---

> **最后更新**：2026-05-07（基于 PR #10、#11、#12、#13 的最终实现）
