# 数据库设计

## 一、当前状态枚举

`WorkItemStatus` 当前已收敛为：

```prisma
enum WorkItemStatus {
  DRAFT
  PENDING_DECOMPOSE
  PROPOSING
  IN_PROGRESS
  ADJUSTING
  CANCELLING
  COMPLETING
  COMPLETED
  CANCELLED
}
```

## 二、审批类型枚举

```prisma
enum ApprovalType {
  PROPOSE
  ADJUST
  CANCEL
  COMPLETE
}
```

## 三、审批辅助字段

`WorkItem` 上与审批流转相关的字段：

```prisma
beforeApprovalStatus WorkItemStatus?
approvalType         ApprovalType?
currentApproverId    Int?
currentApproverRole  Role?
```

字段规则：

1. 进入审批态时写入 `beforeApprovalStatus` 和 `approvalType`。
2. 审批退回时恢复 `beforeApprovalStatus`。
3. 审批结束时清空审批辅助字段和当前审批人字段。

## 四、旧状态迁移映射（历史说明）

第六阶段采用清库重建方案 A，不对测试库执行生产级 backfill。以下映射仅作为历史数据迁移参考，不再作为当前运行口径：

| 旧状态 | 新状态 |
| --- | --- |
| `DRAFT` | `DRAFT` |
| `PENDING_DEPT` | `PROPOSING` |
| `PENDING_COMPANY` | `PROPOSING` |
| `APPROVED` | `IN_PROGRESS` |
| `IN_PROGRESS` | `IN_PROGRESS` |
| `PENDING_DECOMPOSE` | `PENDING_DECOMPOSE` |
| `PENDING_COMPLETE` | `COMPLETING` |
| `PENDING_EVIDENCE_DEPT` | `COMPLETING` |
| `PENDING_EVIDENCE_COMPANY` | `COMPLETING` |
| `PENDING_MAIN_LEADER_CANCEL` | `CANCELLING` |
| `COMPLETED` | `COMPLETED` |
| `REJECTED` | `DRAFT` |
| `ADJUSTING` | `ADJUSTING` |
| `CANCELLING` | `CANCELLING` |
| `CANCELLED` | `CANCELLED` |

## 五、系统用户字段

| 字段 | 说明 |
| --- | --- |
| `creatorId` | 系统创建人 / 系统填报人 / 经办账号 |
| `firstSubmitterId` | 首次提交人，用于立项审批退回后的处理参考 |
| `currentApproverId` | 当前审批节点用户 |
| `currentApproverRole` | 当前审批节点角色 |
| `proposedLeaderId` | 待办提出领导或相关公司领导 |
| `approvalLeaderId` | 审批领导，后续应明确与 `proposedLeaderId` 的职责边界 |
| `responsibleLeaderUserId` | 责任领导系统用户，FK → users |
| `responsiblePersonUserId` | 责任人系统用户，FK → users |

## 六、业务责任人字段

### 重点工作 / 主要工作

```prisma
responsibleLeader  String?  @db.VarChar(50)
responsiblePerson  String?  @db.VarChar(50)
```

### 待办事项（Phase 8B 统一）

```prisma
departmentId       Int?
responsibleLeader  String?  @db.VarChar(50)
responsiblePerson  String?  @db.VarChar(50)
cooperators        Json?
```

已删除字段：`responsibleDepartmentIds`、`cooperateDepartmentIds`、`responsiblePersons`、`cooperatePersons`。

## 七、部门字段

`cooperators` JSON 结构：
```json
[
  {
    "departmentId": 3,
    "departmentName": "工艺技术处",
    "leader": "王主任",
    "person": "赵工"
  }
]
```

- `departmentId` 用于配合部门可见范围判断
- `departmentName` 为快照字段，可选
- `leader` / `person` 为姓名文本快照，可选，不参与权限判断

`getResponsibleDepartmentIds(work)` 返回 `work.departmentId ? [work.departmentId] : []`。
`getCooperatorDepartmentIds(work)` 从 `work.cooperators` 解析 `departmentId` 并返回配合部门 ID 数组。

## 八、Member / DepartmentMember 说明

本轮不强制新增 `Member` 或 `DepartmentMember` 表。

如果后续引入，`Member` / `DepartmentMember` 应表示“部门人员名录 / 业务人员名录”，不等于普通员工登录账号。成员可以是部门领导、部门副职、专业负责人、普通业务人员，也可以不登录系统。责任领导和责任人都可以来自成员名录，但成员名录不天然产生审批、办理或可见权限。

## 九、清库与迁移

第六阶段 PR 6.1 采用清库重建方案 A：删除旧测试数据，执行 9 状态 schema migration，并重新 seed 目标 9 状态测试数据。

即使采用清库路径，也必须保留：

1. 旧状态到新状态映射。
2. 目标字段设计。
3. 测试数据重建方式。
4. 未来正式数据迁移参考说明。

如果后续不能清库，则需要一次性迁移脚本处理状态、审批辅助字段、部门字段和责任人字段；旧状态映射以上表为准。
