# 待修复问题

本文只记录当前 target-contract 或阶段边界下仍需后续处理的问题。已经在 Phase 2 至 PR 6.4 收口并通过验证的事项不再列为待修复。

## 当前验证状态

第六阶段 PR 6.4 收尾后，目标应保持：

```text
PASS 108
EXPECTED_FAILURE 0
UNEXPECTED_PASS 0
FAIL 0
```

以实际运行输出为准；若后续新增 contract 用例，PASS 数可能增加，但不应重新出现普通 FAIL 或 EXPECTED_FAILURE。

## 已完成，不再列为待修复

1. `/api/works` 可见范围统一。
2. `VICE_PRESIDENT` / `PRESIDENT` 默认可见范围收窄。
3. 部门多主责 / 配合部门可见范围统一。
4. 完成率部门 B 多主责统计。
5. `/api/dashboard/summary` 接入统一权限范围、待审批 / 待办理 helper 和 7 天临期窗口。
6. `GET /api/dashboard` 与首页轻量列表调用链。
7. 普通 Excel 导入导出权限、字段和状态文案口径。
8. 9 状态 schema 与状态元数据迁移。
9. workflow 新状态流转与审批权限同步。
10. 页面、接口、状态筛选、Excel 状态口径切换到 9 状态。
11. “退回待修改”通过 `DRAFT` + 退回痕迹派生展示，不再依赖 `REJECTED`。
12. `tests/regression-baseline.cjs` 已重写为 9 状态轻量回归脚本。

## 当前仍需后续处理

1. **SUPERVISOR 督办跟踪与普通待处理口径拆分**
   - 当前 `SUPERVISOR` 仍有“督办跟踪”特殊展示语义。
   - 后续应拆为独立字段、独立列表或独立页面，不再与普通用户“待处理 = 待审批 + 待办理”混用。

2. **业务人员体系增强**
   - `responsiblePersons`、`cooperatePersons`、`responsibleLeader`、`responsiblePerson` 当前仍作为姓名文本。
   - 本轮不新增 `Member`、`DepartmentMember`、`WorkParticipant`。人员重名、调岗、无法按人员 ID 统计等风险留待后续评估。

3. **发布前综合验收**
   - 第六阶段完成后，仍需按 `docs/回归测试清单.md` 和 `docs/测试发布检查清单.md` 执行完整发布前验证。
