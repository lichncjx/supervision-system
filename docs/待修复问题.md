# 待修复问题

本文档记录目标口径自动化验证脚本当前预期会暴露的问题。这里的问题不表示脚本错误，而是当前接口与目标业务规则之间的差异。

截至第六阶段 PR 6.2，第二阶段已经收口的 `/api/works` 可见范围、`VICE_PRESIDENT` 默认可见范围、部门多主责 / 配合部门可见、完成率部门 B 多主责统计，第三阶段收口的 `/api/dashboard/summary` 统计口径，第四阶段收口的 `GET /api/dashboard` 首页调用链，第五阶段收口的普通 Excel 导入导出权限和字段口径，PR 6.1 完成的 9 状态 schema / 状态元数据迁移，以及 PR 6.2 完成的 workflow 新状态流转，均不再列为业务待修复问题。

验证脚本：

```bash
TARGET_CONTRACT_ENV=local TARGET_CONTRACT_RESET=1 node tests/target-contract/run-target-contract.cjs --base-url http://localhost:5000
```

安全要求：

1. `TARGET_CONTRACT_ENV` 必须显式设置为 `local` 或 `test`。
2. `TARGET_CONTRACT_RESET=1` 是清库 seed 的强制条件。
3. `DATABASE_URL` 必须指向本地主机数据库，允许 host：`localhost`、`127.0.0.1`、`::1`、`db`、`postgres`。
4. 脚本拒绝包含 `supabase.co`、`pooler.supabase.com`、`vercel`、`production`、`prod`、`preview` 的数据库 URL。
5. 脚本拒绝在 `VERCEL` / `VERCEL_ENV` 环境中运行。
6. verify 脚本只允许调用本地 `http://localhost` / `http://127.0.0.1` / `http://[::1]` 服务。
7. 脚本会输出脱敏后的 `DATABASE_URL`，用于人工确认没有误连生产库、Supabase Preview 库或 Vercel 环境库。

## 一、第四阶段已修复：Dashboard 统一接口与首页调用链

第四阶段已新增目标接口：

```http
GET /api/dashboard
```

该接口一次返回：

1. `summary`：首页卡片统计。
2. `lists.expiringAndOverdue`：临超期轻量列表。
3. `lists.myActionRequired`：待处理轻量列表。

第三阶段已收敛 `GET /api/dashboard/summary` 的卡片统计口径：

1. summary 统计范围已复用服务端统一权限范围 helper。
2. `VICE_PRESIDENT` / `PRESIDENT` 不再在 summary 中默认按全局统计。
3. summary 待审批 / 待办理已复用 `canApproveWorkItem` / `canHandleWorkItem`。
4. PR 6.1 后 `APPROVED` 已取消，`inProgress` 只以 `IN_PROGRESS` 作为当前运行口径。
5. 临期已按 7 天窗口计算，接口新增 `expiring`，迁移期保留 `thisMonthDue` 兼容别名。

当前 target-contract 中 `GET /api/dashboard/summary` 已按正常目标断言处理，不再标记为 `EXPECTED_FAILURE`。

第四阶段已完成：

1. `GET /api/dashboard` 和 `GET /api/dashboard/summary` 已复用同一个 summary 计算 helper。
2. 首页已改为调用 `GET /api/dashboard`，不再通过 `/api/works` 或 `getVisibleWorks(user)` 拉全量事项后前端计算临超期和待处理列表。
3. `lists.expiringAndOverdue` 只返回轻量字段，并与 summary 的 `expiringCount` / `overdueCount` 口径一致。
4. `lists.myActionRequired` 只返回 `canApproveWorkItem` / `canHandleWorkItem` 命中的事项，并与 `pendingApprovalCount` / `pendingHandlingCount` / `myActionRequiredCount` 口径一致。
5. target-contract 已新增 `GET /api/dashboard` 断言并全部通过。

仍待后续阶段处理：

1. `SUPERVISOR`“督办跟踪”列表仍是特殊展示口径，尚未拆分为独立字段或独立列表，不能与普通用户“待处理”混用。
2. 完成率仍由 `GET /api/dashboard/completion-rate` 和 `GET /api/excel/completion-rate` 独立提供，第四阶段未强制并入 `GET /api/dashboard`。

## 二、第二阶段已修复：事项列表可见范围

第二阶段前，以下目标口径差异曾作为 expected failure 跟踪：

```http
GET /api/works
```

1. `VICE_PRESIDENT` / `PRESIDENT` 曾可能按公司级角色查看过宽数据。
2. 部门用户曾主要按标量 `departmentId` 过滤，未完整覆盖多主责部门 `departmentIds` 和配合部门 `cooperateDepartmentIds`。
3. 目标口径要求 `responsiblePersons` / `cooperatePersons` 不参与权限判断；脚本会验证姓名相同不能授权。
4. 目标口径要求 `responsibleDepartmentIds` / `departmentIds` / `departmentId` 和 `cooperateDepartmentIds` 参与组织权限判断。

第二阶段已修复并接入 `src/lib/server-permissions.ts`：

1. `ADMIN` / `SUPERVISOR` 全局可见。
2. 部门角色按主责部门、配合部门、当前审批人可见。
3. `VICE_PRESIDENT` 默认按本人提出、本人审批/负责、当前审批人可见。
4. `PRESIDENT` 默认按本人提出、本人审批/负责、当前审批人、主要领导决策事项可见。

收口后，以下 target-contract 断言已从 expected failure 转为正常 `PASS`：

1. `/api/works` 公司领导可见范围不再过宽。
2. `VICE_PRESIDENT` 默认不能看到其他副总事项。
3. 部门多主责 `departmentIds` 可授予组织可见范围。
4. 配合部门 `cooperateDepartmentIds` 可授予组织可见范围。
5. `responsiblePersons` / `cooperatePersons` 仍不参与权限判断。

## 三、第二阶段已修复：完成率 B 部门多主责统计

第二阶段前：

```http
GET /api/dashboard/completion-rate
```

第二阶段已将 `GET /api/dashboard/completion-rate` 和 `GET /api/excel/completion-rate` 改为通过 `getResponsibleDepartmentIds(work)` 按主责部门集合统计，配合部门不计入完成率分母和分子。

收口后，部门 B / 多主责部门完成率 target-contract 断言已从 expected failure 转为正常 `PASS`。

仍需关注：

1. 当前 schema 尚未迁移 `responsibleDepartmentIds` 字段，查询层仍只能使用 `departmentIds` / `departmentId` 做候选过滤，再在内存中通过 helper 判断。
2. 目标 9 状态 schema 已在 PR 6.1 落地，完成率状态继续基于 `COMPLETED` / `CANCELLED`。

以上关注点不再作为本阶段完成率 B 部门多主责统计的 expected failure；后续会随 `responsibleDepartmentIds` 字段迁移继续收敛。

## 四、当前 target-contract 结果

2026-05-10 第六阶段 PR 6.2 完成 workflow 新状态流转后：

```text
PASS 101
```

当前 target-contract 已无 `EXPECTED_FAILURE`、`UNEXPECTED_PASS` 或普通 `FAIL`。

说明：target-contract 已覆盖 workflow POST 新状态流转路径，包括立项、待分解、调整、取消、完成、退回、草稿取消，以及重点工作取消主要领导节点。

## 五、第五阶段已修复：普通 Excel 导入导出

第五阶段已完成：

1. `GET /api/excel/export` 数据范围与 `GET /api/works` 可见范围一致。
2. 普通导出状态文案使用统一状态元数据。
3. 普通导出主责部门通过 `getResponsibleDepartmentIds(work)` 输出，配合部门通过 `getCooperateDepartmentIds(work)` 输出。
4. 普通导出不再把 `VICE_PRESIDENT` / `PRESIDENT` 默认当作全局导出角色。
5. `POST /api/excel/import/[type]` 默认创建 `DRAFT`，不允许普通导入绕过 workflow 直接设置非草稿状态。
6. 部门用户导入时，主责部门必须包含本部门；配合部门可以填写其他部门，但不产生导入授权。
7. 责任领导、责任人、主责责任人、配合责任人只作为姓名文本导入导出，不校验系统用户。
8. `src/lib/excel-utils.ts` 仍作为模板下载和历史客户端工具的 legacy 文件保留，后续可单独清理未使用函数。

## 六、第六阶段 PR 6.1 / PR 6.2 已完成：9 状态 schema、状态元数据与 workflow 流转

第六阶段 PR 6.1 已完成：

1. `WorkItemStatus` 收敛为 9 状态：`DRAFT`、`PENDING_DECOMPOSE`、`PROPOSING`、`IN_PROGRESS`、`ADJUSTING`、`CANCELLING`、`COMPLETING`、`COMPLETED`、`CANCELLED`。
2. 新增 `ApprovalType`：`PROPOSE`、`ADJUST`、`CANCEL`、`COMPLETE`。
3. `WorkItem` 新增 `beforeApprovalStatus` 和 `approvalType`。
4. `src/lib/work-status.ts` 只维护目标 9 状态元数据。
5. target-contract seed 数据已直接使用 9 状态，不再通过当前状态承载目标语义。

旧状态映射仅保留为历史迁移说明：

| 当前状态 | 目标语义 |
|----------|----------|
| `PENDING_DEPT` / `PENDING_COMPANY` | `PROPOSING` |
| `APPROVED` / `IN_PROGRESS` | `IN_PROGRESS` |
| `PENDING_COMPLETE` / `PENDING_EVIDENCE_DEPT` / `PENDING_EVIDENCE_COMPANY` | `COMPLETING` |
| `PENDING_MAIN_LEADER_CANCEL` / `CANCELLING` | `CANCELLING` |
| `REJECTED` / `DRAFT` | `DRAFT` |

本阶段采用清库重建方案 A，不对旧测试数据做生产级 backfill。

第六阶段 PR 6.2 已完成：

1. `POST /api/works/[id]/workflow` 已按 9 状态实现真实流转。
2. 进入 `PROPOSING`、`ADJUSTING`、`CANCELLING`、`COMPLETING` 时写入 `beforeApprovalStatus` 和 `approvalType`。
3. 多节点审批通过时状态保持当前审批态，只切换 `currentApproverId` / `currentApproverRole`。
4. 审批结束后按 `approvalType` 进入 `IN_PROGRESS`、`CANCELLED` 或 `COMPLETED`，并清空审批辅助字段。
5. 审批退回后恢复到 `beforeApprovalStatus`，并清空审批辅助字段。
6. 草稿取消直接 `DRAFT -> CANCELLED`，不进入审批态。
7. 重点工作取消主要领导审批不再使用 `PENDING_MAIN_LEADER_CANCEL`，而是在 `CANCELLING` 下切换主要领导审批节点。
8. `canApproveWorkItem` / `canHandleWorkItem` 已同步新审批态和办理态。

PR 6.2 后仍待后续阶段处理：

1. 页面和接口状态筛选完整适配仍留到 PR 6.3。
2. 退回事项通过 `DRAFT` + 退回痕迹展示“退回待修改”，仍留到 PR 6.3。
3. `tests/regression-baseline.cjs` 旧状态人工回归脚本仍待 PR 6.4 清理。
4. `SUPERVISOR` 督办跟踪与普通待处理口径拆分继续后置。

## 七、目标责任字段尚未完整落地

当前 schema 尚未提供重点/主要事项的独立 `responsiblePerson` 字段。目标口径为：

1. `responsibleLeader`：事项业务责任领导姓名文本。
2. `responsiblePerson`：事项实际责任人姓名文本。
3. 二者不是 `User` 外键，不是 `Member` 外键，不参与权限判断。

当前测试脚本不会把业务责任人姓名作为权限依据；后续 schema 落地后需要补充字段级断言。
